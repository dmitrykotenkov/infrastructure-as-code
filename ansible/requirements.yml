---
# =============================================================================
# Ansible Galaxy Requirements
# =============================================================================
# This file defines the Ansible collections required for this project
# Install with command: ansible-galaxy collection install -r requirements.yml
# Install with script: /scripts/ensure-ansible.sh
# =============================================================================

collections: []
  # - name: namespace1.name1
  #   version: ">=1.5.0"
  # - name: namespace2.name2
  #   version: ">=3.1.0"
